# Native projects — regenerated fresh via `expo prebuild` before each build,
# not hand-maintained. Keeping them out of git avoids drift from app.json
# (e.g. applicationId/bundleIdentifier getting out of sync with what's
# committed — ios/ had drifted to the placeholder "org.name.Errin").
android/
ios/
